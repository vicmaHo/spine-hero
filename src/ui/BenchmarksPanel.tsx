/**
 * Placeholder para el panel de benchmarks de V.
 * Día 3: V rellena este componente con p50/p95, FPS y frames descartados.
 */
export function BenchmarksPanel() {
  return (
    <div
      data-testid="slot-benchmarks-panel"
      className="w-full h-full min-h-[200px] bg-gray-800 rounded-lg border border-dashed border-gray-600 flex items-center justify-center"
    >
      <span className="text-gray-500 text-xs select-none">Benchmarks Panel (V)</span>
    </div>
  );
}
